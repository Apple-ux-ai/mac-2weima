# 项目待解决问题 (ISSUES)

| 编号 | 问题描述 | 影响范围 | 可选方案 | 推荐方案 | 需要决策的具体内容 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| ISS-001 | 点击“获取授权码”提示“当前环境无需授权码” | 软件激活流程 | 1. 修改前端逻辑，在 is_need_auth_code 为 0 时仍尝试跳转；2. 检查服务端 soft_number 配置。 | 优先确认服务端 soft_number 是否为 10019，若正确则修改前端在点击时强制跳转。 | 确认 soft_number (10019) 是否正确，以及为何服务端返回无需授权。 | 待确认 |
| ISS-002 | 活码跨设备扫描显示“活码未找到” | 活码功能 | 修正 API 地址推导逻辑，避免在 80 端口环境下强制推导 3000 端口；同时在 `viewer.html` 中增加 API 请求失败时的自动降级/备用机制。 | 1. 修改 `api.js` 推导逻辑。2. 增强 `viewer.html` 的 `getContentFromServer` 函数，增加端口自动切换尝试。 | 已完成修复，并生成最终安装包。需确保服务器上的 `viewer.html` 已同步更新。 | 已修复 |
| ISS-005 | 静态码文字重叠问题 | 批量二维码生成 | 1. 缩小二维码尺寸。2. 将二维码上移。3. 文字渲染在二维码下方并留出间距。 | 在 `BatchQRGenerator.vue` 中动态计算布局，当启用文字时自动调整二维码 Y 坐标和尺寸。 | 已在 `generatePreview` 和 `generateSingleQR` 中实现动态布局。 | 已修复 |
| ISS-003 | 扫码端是否必须访问 :3000 端口（部分网络/浏览器可能受限） | 活码/文件查看器（跨设备） | 1. viewer 与 API 统一走 80 端口（`/viewer.html` + `/api` 反代）；2. viewer 走 80，但二维码携带 `?api=http://xxx:3000/api`；3. viewer 与 API 全部改走 :3000（需确保 /viewer.html 可用）。 | 优先方案1：二维码与 viewer 固定使用 `http://111.229.158.50/viewer.html` + `api=http://111.229.158.50/api`，避免暴露/依赖 :3000。 | 需要确认：对外是否统一走 80/443（推荐）。若统一走 80/443，则安装包默认指向 80/443；若保留 :3000，对部分扫码器/网络可能受限。 | 待确认 |
| ISS-004 | 活码创建/编辑时服务器同步失败的处理策略 | 活码功能 | 1. 继续“尽力同步”，失败仅提示但允许本地保存；2. 强制要求同步成功，否则不允许创建/保存；3. 提供“仅本地活码/可跨设备活码”开关。 | 默认采用方案1并显式提示失败，避免用户误以为可跨设备。 | 需要确认：活码是否必须 100% 可跨设备（同步失败就阻止保存），还是允许本地模式。 | 待确认 |
